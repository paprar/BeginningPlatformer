디버깅 하는법

디버깅: 실행은 정상적으로 되지만 내 생각대로 코드가 안되는 오류들을 해결하는 과정


작업 과정

1. 중단점 설정: 소스코드 라인 숫자 오른쪽에서 f9 또는 중단점 삽입

2. 디버그 모드 실행: f5 또는 디버그-디버깅 시작

3. if-else if-else if문법: 특정 조건을 가지고 하면 else if쓰지 else 쓰진 않음. else는 아무런 조건이 없는 남은 경우의 수에만 사용. 근데 그대로 했는데 안됨.